<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/check_history_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light">

        </android.support.v7.widget.Toolbar>


    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:alpha="255"
        android:orientation="horizontal">

        <Button


            android:id="@+id/activity_check_receive_button"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/activity_check_receive_button_bg"
            android:text="@string/receive_history"
            android:textColor="@drawable/activity_selected_text_color"
            android:textSize="15sp" />

        <Button
            android:id="@+id/activity_check_send_button"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/activity_check_send_buttton_bg"
            android:text="@string/send_history"
            android:textColor="@drawable/activity_selected_text_color"
            android:textSize="15sp" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/activity_check_history_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>