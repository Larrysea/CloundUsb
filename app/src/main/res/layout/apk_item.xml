<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="5dp"
    >
    <FrameLayout android:layout_width="wrap_content"
        android:layout_centerInParent="true"
        android:id="@+id/apk_framelayout"
        android:layout_marginBottom="10dp"
        android:layout_height="wrap_content">
        <ImageView
            android:id="@+id/apk_item_image_view"
            android:layout_marginTop="8dp"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:scaleType="fitXY"

            android:layout_gravity="top"
        />
    <CheckBox
        android:id="@+id/apk_item_check_box"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_gravity="right"
        android:button="@null"
        android:visibility="gone"
        android:background="@drawable/child_picture_checkbox"/>
   </FrameLayout>

    <TextView

        android:id="@+id/apk_item_name_TextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:singleLine="true"
        android:textSize="12sp"
        android:gravity="center"

        android:layout_below="@id/apk_framelayout"
        android:textColor="@color/black"
        android:layout_marginTop="20dp"
        />
</RelativeLayout>

